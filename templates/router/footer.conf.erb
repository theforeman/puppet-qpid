
address {
    prefix: /closest
    fanout: single
    bias: closest
}

address {
    prefix: /unicast
    fanout: single
    bias: closest
}

address {
    prefix: /exclusive
    fanout: single
    bias: closest
}

address {
    prefix: /multicast
    fanout: multiple
}

address {
    prefix: /broadcast
    fanout: multiple
}

address {
    prefix: /
    fanout: multiple
}
